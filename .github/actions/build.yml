name: 'Build for PocketMage'
description: 'Build your app for the PocketMage'

inputs:
  upload_artifact:
    description: 'Whether to upload the built firmware artifact'
    required: false
    default: 'true'

runs:
  using: "composite"
  steps:
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Cache PlatformIO dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/pip
          ~/.platformio/.cache
        key: ${{ runner.os }}-pio-${{ hashFiles('exampleApp/platformio.ini') }}
        restore-keys: |
          ${{ runner.os }}-pio-

    - name: Install PlatformIO Core
      shell: bash
      run: pip install --upgrade platformio

    - name: Build PlatformIO Project
      shell: bash
      working-directory: exampleApp
      run: pio run

    - name: Prepare exampleApp Package
      shell: bash
      working-directory: exampleApp
      run: |
        # Rename icon
        cp exampleApp/exampleApp_ICON.bin exampleApp_ICON.bin

        # Rename firmware
        cp .pio/build/*/firmware.bin exampleApp.bin

        # Create uncompressed tar
        tar -cf exampleApp.tar exampleApp.bin exampleApp_ICON.bin

    - name: Update Latest Build
      shell: bash
      working-directory: exampleApp
      run: |
        mkdir -p latest
        cp exampleApp.tar latest/

    - name: Upload exampleApp Package
      if: inputs.upload_artifact == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: TactileexampleApp_pocketmage
        path: exampleApp/exampleApp.tar
        retention-days: 30